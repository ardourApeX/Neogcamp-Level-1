// Take an array with 5 colors. Create another array by adding two more colors into it
const fiveColors = ["Green", "Red", "Blue", "Purple", "Pink"]
const sevenColors = [...fiveColors, "Gray", "Black"]
console.log(sevenColors);
console.log(fiveColors);